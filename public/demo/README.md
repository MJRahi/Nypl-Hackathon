# Committed demo fixtures, keyed by BBL.
# resolveReport() checks this directory before the cache and before any network call,
# so a demoed building renders with the wifi unplugged.
# Generated by scripts/warm-cache.ts (step 8).
